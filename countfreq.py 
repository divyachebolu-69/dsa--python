def count_frequency(arr):
    freq = {}
    for item in arr:
        freq[item] = freq.get(item, 0) + 1
    return freq

arr = [1, 2, 2, 3, 3, 3, 4]
print("Array:", arr)
print("Frequencies:", count_frequency(arr))